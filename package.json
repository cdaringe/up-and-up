{
  "devDependencies": {
    "elm-format": "^0.8.1",
    "husky": "^1.1.3",
    "lint-staged": "^8.0.4",
    "prettier-standard": "^8.0.1",
    "standard": "^12.0.1"
  },
  "scripts": {
    "format": "prettier-standard 'src/**/*.js'",
    "precommit": "lint-staged",
    "lint": "standard"
  },
  "lint-staged": {
    "linters": {
      "src/**/*.js": [
        "prettier-standard",
        "git add"
      ]
    }
  }
}
